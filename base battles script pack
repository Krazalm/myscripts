
for i,v in next, getgc(true) do
   if type(v) == "table" and rawget(v, "damage") then
       v.maxAmmo = math.huge
       v.bloomFactor = 0
       v.noYawRecoil = "true"
       v.automatic = "true"
       v.recoilCoefficient = 0
   end
end
